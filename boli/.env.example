# Server-only. Never prefix this value with PUBLIC_.
TARKA_API_KEY=tk_live_replace_me
TARKA_API_BASE_URL=https://tarka.rest/v1

# Safe to expose to the browser.
PUBLIC_POCKETBASE_URL=https://pb.boli.tarka.site

# Only required when running `npm run setup:pocketbase`.
# Keep these in your shell, not in a committed .env file.
# PB_SUPERUSER_EMAIL=
# PB_SUPERUSER_PASSWORD=
